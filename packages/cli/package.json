{
  "name": "commandscenter",
  "version": "0.26.1",
  "type": "module",
  "description": "Single-user AI specialist management — install, run, done.",
  "license": "Apache-2.0",
  "author": "maestroerror",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MaestroError/commands-center.git",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/MaestroError/commands-center",
  "bugs": {
    "url": "https://github.com/MaestroError/commands-center/issues"
  },
  "bin": {
    "ccenter": "./dist/bin.mjs"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "node --import tsx/esm build.ts",
    "start": "node dist/bin.mjs start",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@fastify/static": "^9.1.3",
    "better-sqlite3": "^12.0.0",
    "fastify": "^5.8.5",
    "opencode-ai": "~1.17.20"
  },
  "devDependencies": {
    "@cc/backend": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "catalog:",
    "tsx": "^4.19.0"
  }
}

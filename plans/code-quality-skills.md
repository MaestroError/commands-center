# Code Quality Skills Plan

1. Add `code-review`, `code-security`, and `code-simplification` built-in OpenCode skills under `packages/backend/resources/builtinSkills/`.
2. Keep each skill concise, action-oriented, and aligned with the existing built-in skill structure.
3. Update catalog tests so the new skill slugs are expected in sorted order.
4. Run formatting, lint, and targeted tests before reporting completion.

# Documents Tree Collapsed By Default Plan

1. [ ] Inspect the Documents sidebar tree code and identify where nested folder open state is initialized.
2. [ ] Change folder nodes so they start closed by default while preserving manual toggles and the top-level Documents section behavior.
3. [ ] Update sidebar tests to reflect the new default collapsed state and cover expanding folders explicitly.
4. [ ] Run `eslint --fix` and the relevant frontend tests before reporting completion.

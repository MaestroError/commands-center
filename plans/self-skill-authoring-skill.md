# Self Skill Authoring Built-In Skill Plan

1. Inspect the built-in skill loading path to determine whether adding a skill directory is enough or whether registry metadata also needs updates.
2. Add a new built-in skill for specialists creating private OpenCode skills inside their own workspace, reusing the authoring principles from `global-skill-authoring` but orienting storage and next steps around the specialist itself.
3. Run formatting, `eslint --fix`, and tests, then review the diff.
